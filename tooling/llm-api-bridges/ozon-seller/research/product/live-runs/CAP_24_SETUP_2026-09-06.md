# CAP-24 — SKU monthly unit economics orchestration — SETUP

Date: 2026-09-06
Status: `ACTIVE`

Primary-gate authority:
`OZON_AI_WORKER_UNIT_ECONOMICS_CAPABILITY_REQUIREMENT_2026-09-06.md`

Canonical business job:
`Возьми один реально продававшийся товар из моего текущего каталога и посчитай по нему юнит-экономику за последний полный календарный месяц. Покажи продажи в штуках и рублях, все расходы Ozon, которые можно честно привязать к этому SKU: комиссию за продажу, логистику/доставку, эквайринг, возвраты/сторно, размещение и другие прямые удержания, а также рекламные расходы. Затем посчитай, сколько осталось после этих расходов всего и на одну проданную единицу. Если какой-то расход Ozon доступен только общей суммой по кабинету или его нельзя доказуемо привязать к SKU, не распределяй его выдуманным способом — покажи отдельно как нераспределённый расход/coverage gap и объясни, чего не хватает для точного расчёта.`

Frozen period:
- `2026-08-01` through `2026-08-31` inclusive.

Core discipline:
- select a real SKU from actual August sales evidence;
- use actual period revenue/units, never current price × units when period evidence exists;
- preserve Ozon SKU / Seller product_id / offer_id identity;
- use finance evidence only where SKU/posting attribution is defensible;
- do not allocate account-level/non-item costs to the SKU without an explicit attribution key;
- advertising/placement are included only where defensible SKU attribution exists;
- prevent double counting across analytics, postings and finance;
- label result as Ozon-side contribution / marketplace unit economics, not full net profit unless seller COGS/tax and other non-Ozon costs are supplied;
- one explicit command -> at most one physical business request; no hidden pagination/fanout/retry.

## Run 01 contract verification

Active v0.1.19 contract confirms for `analytics_data`:
- required `date_from`, `date_to`, `dimension`, `metrics`, `limit`;
- dimension `sku` is an allowed universal dimension;
- metrics `revenue` and `ordered_units` are universal metrics;
- sort items may use an analytics metric key with `ASC|DESC` order;
- limit is allowed from 1 through 1000;
- optional offset is non-negative.

Therefore Run 01 will request the full August SKU sales ranking in one explicit provider read with `limit=1000` and `revenue DESC`. Its purpose is to select a genuinely sold SKU from provider evidence rather than assuming one manually.

## Frozen sequence after Run 01

1. Run 01 — August revenue/ordered-units by SKU; select a real sold SKU.
2. Resolve/confirm product identity only if the selected SKU is not already linked by preserved product authority.
3. Obtain attributable finance deductions for the selected SKU/postings over August using the minimum supported finance reads.
4. Reconcile commission, logistics/delivery, acquiring, returns/reversals and other direct SKU-linked Ozon charges without double counting.
5. Attempt advertising attribution only through a defensible Performance SKU/campaign linkage; otherwise preserve `ATTRIBUTION_NOT_PROVEN`.
6. Attempt placement/storage attribution only through product-level evidence that can be consumed; otherwise preserve the coverage boundary.
7. Calculate attributable Ozon costs, contribution total/per unit and Ozon cost share; keep unallocated costs separate.

No downstream operation is frozen until Run 01 identifies the exact sold SKU and returned sales semantics.

Checkpoint: `CAP_24_ACTIVE_RUN_01_AUGUST_SKU_SALES_NEXT`
