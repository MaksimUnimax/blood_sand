# CAP-21 — Own-card SEO / semantic core — SETUP

Status: `ACTIVE`

Canonical job:

`Собери семантическое ядро для моей карточки SKU 1636048691 и покажи, какие поисковые запросы я недопокрываю. Используй только фактический контент моей карточки и Ozon query/search analytics; не придумывай рыночные запросы, не делай конкурентный benchmark и не заявляй точную поисковую позицию там, где Seller API её не доказывает.`

Selected own SKU: `1636048691` — preserved CAP-19 linkage identifies it as the own product `Славянский оберег - Подвеска на зеркало в машину "Печать Велеса"`.

## Exact Bridge contract authority

Current v0.1.19 operation registry / implementation bindings on branch `repair/ozon-date-contract-2026-09-04` prove:

1. `product_info_description`
   - Seller API `POST /v1/product/info/description`
   - all accounts
   - current READ / safe projection
   - exactly one of `offer_id` or `product_id`
   - `product_id` is a safe int64 number
   - purpose: obtain the description of one product

2. `seller_product_attributes`
   - Seller API `POST /v4/product/info/attributes`
   - current READ / safe projection
   - supports `filter.product_id` as an explicit int64 identifier array
   - explicit `limit`; no hidden pagination

3. `product_queries`
   - Seller API `POST /v1/analytics/product-queries`
   - current READ / safe projection
   - required params: `date_from`, `page_size`, `skus`
   - optional: `date_to`, `page`, `sort_by`, `sort_dir`
   - `page_size <= 1000`; up to 1000 SKUs
   - all accounts receive partial response; history older than one month is subscription-gated
   - contract state `official_limits_v2_1`

4. `product_queries_details`
   - Seller API `POST /v1/analytics/product-queries/details`
   - current READ / safe projection
   - required params: `date_from`, `page_size`, `skus`, `limit_by_sku`
   - optional: `date_to`, `page`, `sort_by`, `sort_dir`
   - `page_size <= 100`, `limit_by_sku <= 15`
   - all accounts receive partial response; history older than one month is subscription-gated; some sort modes have additional subscription rules
   - contract state `official_limits_v2_1`

Premium-Pro-only marketplace-wide search-query endpoints are **not** substituted for CAP-21. CAP-21 is an own-card test and should remain on own-card + own-product-query evidence.

## Frozen method

1. Read the exact current text description of SKU `1636048691` via one explicit `product_info_description` call using `product_id: 1636048691`.
2. Read the current structured attributes for that same own product via a separate explicit `seller_product_attributes` call only if needed after the description result.
3. Read recent own-product query summary with `product_queries` for the complete recent window `2026-08-07T00:00:00Z` through `2026-09-05T23:59:59Z`; this stays within the non-history-gated one-month boundary. Use explicit SKU `1636048691`. No hidden pagination.
4. Read query detail with `product_queries_details` for the same SKU/window, using explicit `limit_by_sku <= 15`; no subscription-restricted sort is requested unless live evidence proves it is needed and entitled.
5. Construct the semantic core only from terms evidenced by the card itself and Ozon own-product query analytics.
6. Mark an undercoverage/gap only when the Ozon query evidence and own-card content support that conclusion. Do not invent marketplace phrases.
7. Keep boundaries explicit:
   - own-card content = CAP-21 authority;
   - own-product query analytics = CAP-21 authority;
   - competitor benchmark = reserved for CAP-22;
   - exact marketplace/category position or coverage boundary = reserved for CAP-23.
8. One `OZON_API_V1` command -> at most one physical business request per run.
9. On provider 429/4xx/5xx, preserve failure and recover the same step; no skipping.

Scoring: standard CAP fields. Key proof is that the worker can combine real own-card content with real Ozon query evidence into an evidence-backed semantic core and identify only defensible coverage gaps.

Checkpoint: `CAP_21_ACTIVE_RUN_1_NEXT`
