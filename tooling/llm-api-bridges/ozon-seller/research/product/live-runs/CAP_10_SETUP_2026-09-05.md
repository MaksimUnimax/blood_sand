# CAP-10 — Prices / price details — SETUP

Status: ACTIVE

Canonical job:

`Какие текущие цены установлены на товары моего каталога? Покажи цену по каждому SKU и выдели позиции, где Ozon отдаёт разные типы цены, скидочные или иные ценовые признаки. Используй отдельные данные Ozon о ценах, а не продажи или ручной экспорт.`

Capability target: prove the worker recognizes current product pricing as a dedicated Ozon data surface and can inspect the entire current catalog without asking the operator for a manual price export.

Fresh catalog authority: CAP-01 current catalog contains 76 SKUs.

Contract: `product_price_details` requires `skus`; accepted range is 1..1000 int64 identifiers, so all 76 current catalog SKUs fit one explicit business request.

Method:
1. Request `product_price_details` for all 76 current catalog SKUs.
2. Use only provider-returned price/discount/index fields; do not invent semantic meanings for absent fields.
3. If provider omits any requested SKU, distinguish omission from a zero price and investigate before closing the row.

Checkpoint: `CAP_10_ACTIVE_RUN_1_NEXT`
