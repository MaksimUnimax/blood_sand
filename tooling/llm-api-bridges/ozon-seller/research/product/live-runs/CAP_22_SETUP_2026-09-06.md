# CAP-22 — Competitor SEO / positioning benchmark — SETUP

Status: `ACTIVE`

Date: 2026-09-06

Canonical job:

`Найди релевантных конкурентов моего товара SKU 1636048691 на Ozon и сравни их карточки с моей: семантика, заголовок, описание, характеристики и цена. Используй конкурентов только после доказанного Ozon-дискавери; private seller evidence держи отдельно от public competitor-card evidence; не приписывай конкурентам продажи, конверсию или другие private metrics и не заявляй полноту рынка.`

Selected own product authority from completed CAP-21:
- SKU `1636048691`;
- Seller product_id `1119965443`;
- offer_id `Печать Велеса`;
- title `Славянский оберег - Подвеска на зеркало в машину "Печать Велеса"`;
- own-card description/attributes/content-rating/query evidence are preserved in CAP-21 and remain private Seller evidence.

Controlling methodology authority:
`OZON_AI_WORKER_SEO_COMPETITIVE_POSITION_CAPABILITY_REQUIREMENT_2026-09-02.md`.

## Exact current Bridge contract authority

The active v0.1.19 node bundle on branch HEAD exposes all-account read-only pricing-strategy competitor surfaces:

1. `pricing_strategy_product_info`
   - Seller API `POST /v1/pricing-strategy/product/info`;
   - READ_SAFE / safe projection / current;
   - exact params: only `product_id`;
   - `product_id` must be a safe JSON integer number;
   - purpose: return competitor price and competitor product link for a product in a pricing strategy;
   - URL is returned as data only and is not automatically opened;
   - entitlement: ALL_ACCOUNTS.

2. `pricing_strategy_ids_by_product_ids`
   - Seller API `POST /v1/pricing-strategy/strategy-ids-by-product-ids`;
   - READ_SAFE / current;
   - exact params: `product_id` array, maximum 50;
   - each product_id must be string-form int64;
   - purpose: resolve assigned pricing-strategy IDs for explicit own product IDs without fanout;
   - entitlement: ALL_ACCOUNTS.

3. `pricing_strategy_competitors`
   - Seller API `POST /v1/pricing-strategy/competitors/list`;
   - READ_SAFE / current;
   - exact params: `page`, `limit`;
   - page is integer >= 1; limit is explicit and no hidden pagination is permitted;
   - purpose: list competitors used by pricing strategies;
   - entitlement: ALL_ACCOUNTS.

The Bridge also exposes strategy list/info/products surfaces, but they are only used if the direct own-product discovery path does not prove a usable competitor.

## Frozen method

1. Start with the minimum direct Ozon competitor discovery read: `pricing_strategy_product_info` for own Seller product_id `1119965443`.
2. If it returns a competitor product URL/price, treat that object only as Ozon competitor-discovery evidence. Do not infer complete market coverage or competitor private sales metrics.
3. If direct product info does not prove a competitor because the item has no pricing-strategy competitor linkage, resolve strategy linkage with `pricing_strategy_ids_by_product_ids`; use strategy/list/competitor reads only where live evidence justifies them. Never hand-pick a competitor to rescue the row.
4. After a specific competitor Ozon URL/product has been proven by Bridge evidence, public web may be used to inspect that competitor's public card. Keep evidence classes explicit:
   - PRIVATE_OWN_SELLER_EVIDENCE — own Seller/API data;
   - OZON_COMPETITOR_DISCOVERY_EVIDENCE — competitor URL/price surfaced by Seller API;
   - PUBLIC_COMPETITOR_CARD_EVIDENCE — publicly observable title/content/attributes/price;
   - INFERENCE — comparison/recommendation, never presented as competitor private metrics.
5. Compare only supported dimensions: semantics, title, description/content, public attributes, observable price positioning, and relevant card presentation.
6. Do not claim competitor sales, conversion, ad performance, search rank or private metrics unless a dedicated Ozon API explicitly proves them.
7. Do not claim discovered competitors are the complete organic competitor set; pricing-strategy competitors are a discovery source, not a market census.
8. If no Ozon competitor can be discovered through current Bridge evidence, close with explicit `COMPETITOR_DISCOVERY_COVERAGE_BOUNDARY`; do not substitute a manually selected marketplace card.
9. One `OZON_API_V1` command -> at most one physical business request. No hidden pagination/fanout/retry.

Scoring additionally records `competitor_discovery`, `public_competitor_comparison`, `entitlement_boundary_handled_correctly`, and `unsupported_claims`.

Checkpoint: `CAP_22_DIRECT_PRICING_STRATEGY_PRODUCT_INFO_NEXT`
