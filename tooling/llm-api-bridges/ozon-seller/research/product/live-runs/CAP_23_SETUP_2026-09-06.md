# CAP-23 — Category/search position & coverage boundary — SETUP

Date: 2026-09-06
Status: `ACTIVE`

Canonical business job:

`Какое место занимает мой товар SKU 1636048691 по ключевым запросам и в категории, кто выше и что мешает подняться? Используй Standard-live evidence where available; не придумывай Premium-only position/category metrics; отдельно покажи, что доступно сейчас, что gated, что отсутствует в Bridge registry, и что нельзя доказать.`

Target own-product authority from CAP-21:
- SKU `1636048691`
- Seller product_id `1119965443`
- offer_id `Печать Велеса`
- CAP-21 proved a queryable control day `2026-08-29` for `product_queries` and `product_queries_details`.

Controlling authority:
- `OZON_AI_WORKER_SEO_COMPETITIVE_POSITION_CAPABILITY_REQUIREMENT_2026-09-02.md`
- `OZON_AI_WORKER_PRIMARY_GATE_CONTINUOUS_RUN_PLAN_2026-09-05.md`

## Known preserved evidence entering CAP-23

From CAP-21 on `2026-08-29`:
- `product_queries` HTTP 200 for SKU `1636048691`;
- `unique_search_users = 4876`;
- `gmv = 1244 RUB`;
- `position = null`;
- `unique_view_users = null`;
- `view_conversion = null`;
- `product_queries_details` returned 15 factual query rows across two explicit pages, but row-level `position` remained null.

This evidence is reusable and must not be discarded or relabelled as zero position.

## Coverage / entitlement authority

Current requirement authority states:
- own-product `product_queries` / `product_queries_details` are the Standard-available search evidence path, subject to readiness/partial-response boundaries;
- Seller Analytics metric `position_category` is Premium Plus/Pro restricted in the accepted entitlement model;
- marketplace-wide `/v1/search-queries/top` and `/v1/search-queries/text` are Premium Pro in the accepted entitlement model;
- live API-key roles include `/v1/analytics/category/comparison`;
- current accepted Bridge registry does not expose an allowlisted operation for `/v1/analytics/category/comparison`.

Required coverage finding if unchanged:

`PROVIDER_ROLE_EXPOSES_CATEGORY_COMPARISON_BUT_BRIDGE_REGISTRY_DOES_NOT`

## Frozen method

1. Run one explicit `product_queries` request on the proven-queryable day `2026-08-29` for SKU `1636048691`, explicitly sorting by `BY_POSITION` with `ASCENDING` direction. This is the minimum live position-focused probe and avoids the known recent-data readiness boundary.
2. Interpret `position` only if provider returns a non-null value. Null is `POSITION_NOT_EXPOSED_IN_STANDARD_PARTIAL_RESPONSE`, not rank zero.
3. Reuse CAP-21 detail evidence rather than re-fetching duplicate pages unless Run 1 materially changes the position evidence.
4. Do not call or simulate Premium-only `position_category` as Standard evidence.
5. Verify/report the Bridge coverage boundary for `/v1/analytics/category/comparison` from current roles + registry authority; do not invent an operation name.
6. Separate final answer components into:
   - `STANDARD_LIVE`
   - `STANDARD_PARTIAL_OR_NULL`
   - `PREMIUM_GATED`
   - `PROVIDER_ROLE_PRESENT_BRIDGE_MISSING`
   - `NOT_PROVEN`
7. One `OZON_API_V1` command -> at most one physical provider request. No hidden pagination/fanout/retry.

## Run 01 exact command

```text
OZON_API_V1
{
  "operation": "product_queries",
  "params": {
    "date_from": "2026-08-29T00:00:00Z",
    "date_to": "2026-08-29T23:59:59Z",
    "page_size": 10,
    "skus": [
      "1636048691"
    ],
    "sort_by": "BY_POSITION",
    "sort_dir": "ASCENDING"
  }
}
```

Checkpoint: `CAP_23_RUN_01_POSITION_FOCUSED_PRODUCT_QUERIES_NEXT`
