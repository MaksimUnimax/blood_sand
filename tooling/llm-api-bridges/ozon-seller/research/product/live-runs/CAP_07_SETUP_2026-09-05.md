# CAP-07 — Supply-order list/status — SETUP

Status: ACTIVE

Canonical job:

`Какие мои поставки сейчас активны и в каких статусах они находятся? Получи список сам из Ozon, не проси меня перечислять supply/order ID вручную.`

Capability target: prove the worker recognizes supply orders as a dedicated logistics surface, can identify the currently non-terminal states, then retrieve the exact active order set without operator-provided IDs.

Freshness rule: historical STD-12 evidence is context only; CAP-07 requires a fresh current read because supply lifecycle states can change.

Planned orchestration:
1. `supply_order_status_counter` to discover which non-terminal states are currently non-zero.
2. `supply_order_list` filtered to those states to obtain exact current active order IDs/status rows and continue pagination until terminal.

Checkpoint: `CAP_07_ACTIVE_STATUS_COUNTER_NEXT`
