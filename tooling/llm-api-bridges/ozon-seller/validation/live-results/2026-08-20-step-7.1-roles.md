# Live ordinary-account validation — Step 7.1 `roles`

Date: 2026-08-20
Branch: `dev/ozon-v0.1.19-manual-delivery-composer-wait-2026-08-18`
Bridge: `0.1.19`
Verdict: **PASS**

Command:
```text
OZON_API_V1
{"operation":"roles","params":{}}
```

Observed live evidence:
- `result_count=1`.
- Batch Seller capability probe: `performed=false`, `status=not_needed`.
- Query planner: `logical_business_result_count=1`, `physical_business_request_count=1`.
- Result operation: `roles`.
- Provider: `seller_api`; HTTP method `POST`.
- `external_request_executed=true`.
- `capability_probe_executed=false`.
- HTTP status `200`.
- Entitlement: `SUPPORTED_AND_ENTITLED`, `capability_required=false`, `partial=false`, reason `operation_not_subscription_sensitive`.
- `command_transformed=false`; logical and physical fingerprints both `b36e23ff`.
- Provider returned a non-empty `roles` array and `expires_at=2027-02-06T08:09:07.738279Z`.

Notable live role coverage in the returned API-key permissions included read-only Product, Posting FBS, Supply order, Returns, Reviews, Questions, Chat, Actions, Pricing strategy and Admin surfaces. The returned Admin method list included the already-tested analytics endpoints and `/v3/posting/fbo/list`, `/v3/supply-order/get`, `/v1/supply-order/details`.

Boundary: this PASS proves the bridge `roles` operation and the live API key's returned permission payload. It does not claim that every method named inside the provider's role payload is implemented as a bridge operation.
