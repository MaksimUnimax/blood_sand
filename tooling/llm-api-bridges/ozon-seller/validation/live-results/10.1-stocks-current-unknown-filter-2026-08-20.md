# Test 10.1 — stocks_current unknown filter boundary — 2026-08-20

## Request

```json
{"operation":"stocks_current","params":{"filter":{"definitely_unknown_field":["x"]},"limit":1}}
```

## Result

- Status: VALIDATION GAP
- Operation executed: `stocks_current`
- External Ozon request: yes
- HTTP status: 200
- Capability probe: not performed
- Bridge/provider path was reached successfully.

## Observation

The bridge did not reject the unknown filter key during command discovery. The request was transformed/executed against Ozon and returned a normal stock response.

This means the current contract boundary does not provide strict local rejection of arbitrary `stocks_current.filter` keys.

## Conclusion

Do not claim strict filter-schema enforcement for `stocks_current`. Treat unknown filter-key handling as an open validation gap.
