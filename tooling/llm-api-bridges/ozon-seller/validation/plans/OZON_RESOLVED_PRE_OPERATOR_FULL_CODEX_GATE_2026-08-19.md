# Ozon Bridge v0.1.19 — resolved standalone pre-operator full Codex gate

Date: 2026-08-19
Status: `READY_TO_DISPATCH_ONE_RESOLVED_FULL_GATE_JOB`

# RESOLVED STANDALONE CODEX PROMPT

Live GitHub is the only source of truth.

Repository:
`MaksimUnimax/blood_sand`

Project:
`tooling/llm-api-bridges/ozon-seller/`

Codex is the independent validator. Production/candidate are immutable.

This job replaces the entire RERUN11-RERUN22 validation-orchestration chain. Do not read, inherit, resume, import or execute any prior RERUN plan/runner/bundle/package. Do not ask the operator for intermediate actions or return intermediate results.

The job has an internal validation-construction stage followed by exactly **one** consolidated candidate gate execution and exactly **one** final report/result.

## Read these three authorities completely and verify their exact blobs

1. Permanent living gate:
   - path: `tooling/llm-api-bridges/ozon-seller/OZON_BRIDGE_PRE_OPERATOR_HANDOFF_FULL_CODEX_GATE.md`
   - exact Git blob: `28c82b263e6cbd01c744cbfc046241837f1d253e`

2. Resolved direct authority + fixed 164-assertion ledger:
   - path: `tooling/llm-api-bridges/ozon-seller/validation/full-gate/FULL_GATE_RESOLVED_AUTHORITY_LEDGER_2026-08-19.md`
   - commit: `7d544b75bf31941af497064d7ae9ce170bda7225`
   - exact Git blob: `852b805b23b10fa53054ee7714ab04222bcc1c6c`

3. Resolved construction/non-fail-fast execution contract:
   - path: `tooling/llm-api-bridges/ozon-seller/validation/full-gate/FULL_GATE_RESOLVED_EXECUTION_CONTRACT_2026-08-19.md`
   - commit: `979b3b963d2f5888f272d389578c05053dc1f482`
   - exact Git blob: `d366b466f32a6213bb4a139b120bf071c203cda4`

Read the direct immutable blobs/docs named by the resolved authority ledger as needed. They are direct sources, not a precedence chain.

## Absolute prohibitions

- Do not modify production or candidate bytes.
- Do not update dependencies/browser versions/system settings.
- Do not use operator Chrome/profile/credentials.
- Do not make real Ozon, Performance, ChatGPT or Alice requests.
- Do not run a prior RERUN top-level runner.
- Do not reuse a prior RERUN authority bundle/workspace/profile/package.
- Do not reuse any prior package ZIP/hash/path.
- Do not pin a historical extension id.
- Do not use historical PASS text as current evidence.
- Do not weaken/remove mandatory assertions.
- Do not publish an intermediate report.
- Do not stop the job and ask the operator to rerun because validation-only harness code has a fixable bug.

Required terminal safety counters:

`REAL_OZON_REQUESTS=0`
`REAL_PERFORMANCE_REQUESTS=0`
`REAL_CHATGPT_REQUESTS=0`
`OPERATOR_BROWSER_ACTIONS=0`

## Stage 1 — internal validation construction/audit, no operator interaction

Create a fresh absolute workspace with a unique nonce. Materialize direct authorities and construct the complete helper suite exactly as required by the resolved execution contract.

This stage may iterate on **validation-only temporary helper/runner code**. If the construction compiler finds a syntax bug, stale path, wrong pin, const rebind, missing evidence mapping, reduced helper, forbidden API, incomplete browser port, package-interlock bug or other validation-only defect, fix it locally and rerun the construction compiler inside this same Codex job.

Do not modify production/candidate and do not publish those iterations.

Construction readiness requires all of the following simultaneously:

- every direct authority identity verified;
- repair/E1/E2 composites verified;
- correct E1 part02 blob `10638ac5c70d07af7f68e51259113e8be63289f4`;
- complete helper suite exists;
- every helper and top-level runner passes `node --check`;
- complete fixed B01-B15 ledger has exactly 164 requirements;
- exact block counts are `11,9,9,10,8,18,7,13,9,10,13,26,10,4,7`;
- every ledger id has executable evidence mapping;
- zero duplicate/unmapped/missing ledger ids;
- no marker-only/source-scan-only substitute for behavioral requirements;
- all mandatory deep audit additions in the resolved ledger are implemented;
- raw-CDP browser helper contains complete E2 + quota-countdown/ChatGPT/Alice + normal-delivery + negative Send-state + owner/wrong-owner + lifecycle assertions;
- all forbidden APIs/patterns from the resolved execution contract are absent;
- no prior RERUN workspace/path/package dependency remains;
- candidate-drift guards exist around all helper families;
- failure aggregation/report serialization is implemented;
- packaging is unreachable until B01-B15 literal PASS.

The construction compiler must inspect **all** items and return a complete defect set, not fail on the first defect. Continue internal validation-only correction until that set is empty.

Print once, immediately before the candidate run:

`OZON_RESOLVED_CONSTRUCTION_AUDIT_PASS`

If a construction defect cannot be repaired without changing production, dependencies, system settings or weakening an assertion, do **not** launch the candidate gate. Publish one final HARNESS/ENVIRONMENT report containing the complete unresolved construction defect set and STOP. Do not request another diagnostic run.

## Stage 2 — exactly one consolidated candidate gate execution

Only after construction audit PASS, create and execute exactly once:

`OZON_RESOLVED_FULL_GATE_RUNNER.mjs`

The runner command is the sole consolidated candidate acceptance execution.

It must execute the resolved execution contract in full:

- Phase A exact frozen artifact + exact repair reconstruction and B01;
- Phase B resolved CFT/no-sandbox/raw-PAGE/direct-worker-CDP substrate;
- Phase C all independent current-candidate non-browser functional helpers;
- Phase D complete current browser/runtime helper;
- mechanical aggregation of all 164 B01-B15 assertions;
- Phase E packaging only after B01-B15 all literal PASS.

### Non-fail-fast rule

Do not terminate the consolidated run on the first ordinary functional/harness/environment assertion failure.

Execute every independent safe helper and assertion that can still run. Record dependencies as `BLOCKED_BY:<id/reason>` where necessary.

Examples:

- if browser substrate fails but candidate reconstruction is valid, still run all non-browser VM/static behavior helpers;
- if one functional block fails, continue independent blocks;
- if one helper child exits nonzero, record its full stdout/stderr/exit and continue independent helpers;
- collect all safely obtainable environment diagnostics in the same run.

Hard abort only for actual safety/integrity breach defined in the resolved execution contract, such as unauthorized external network, immutable source/CFT mutation, candidate drift after freeze or operator profile/credentials exposure. Even then, include all diagnostics already obtainable in the one final report.

## Current behavior that MUST receive explicit current-run evidence

In addition to the complete permanent gate, ensure the following audit-discovered gaps are executed explicitly:

- binding preserved across Manual OFF->ON;
- Seller credentials/settings outside Manual flag preserved;
- cache unchanged;
- quota state, `last_provider_request_at`, `next_allowed_at`, 60000/5000/65000 and seeded Retry-After extension state unchanged;
- cancellation/re-enable zero provider calls;
- new cold-cache same-Seller request after OFF->ON before persisted due: zero provider call and due not shortened; exactly one mocked provider call only after due;
- Manual toggle remains available for pending Manual cancellation when Autorun is not blocker;
- normal empty-composer delivery exactly once;
- recognized staged Send at most once;
- later ordinary user Send untouched;
- disabled Send, Stop, Unknown and Microphone never treated as Send;
- wrong-owner composer never used;
- no global current-conversation assumption;
- Performance-only behavior uses existing Performance host/auth, zero Seller capability probe and no Seller quota/cache semantics;
- lifecycle/restart does not duplicate provider work, insertion or Send;
- full quota-countdown/ChatGPT/Alice/native-Copy/two-owner behavior;
- full current composer-wait E2 behavior including exact banner `Очистите поле ввода, чтобы получить отчёт.`.

## Browser substrate — effective final contract

Use only the resolved environment from the authority ledger:

- Node 24.12.0
- Puppeteer 25.4.0
- CFT 151.0.7922.47
- canonical CFT inventory exactly 308 / `d7b8a2b0c29abcbfba6a0ebfa85ea3296097af3bef45c0b2b60c98055d523b9c`
- fresh full CFT copy
- copied setup once, `shell:false`, exit 78
- exact 11 Chrome args from the resolved authority ledger, including validator-only `--no-sandbox`, no `--disable-gpu-sandbox`, no other switches
- verify actual args BEFORE install
- runtime `browser.installExtension()` + `browser.extensions()` using returned id
- raw PAGE `Target.createTarget`
- PAGE Runtime/Page/Fetch
- Fetch interception installed before supported-origin synthetic navigation
- no `browser.newPage()`
- candidate worker dual-route discovery
- page-session ServiceWorker enable/registration/startWorker exactly once only when absent
- no `triggerAction`
- direct `worker.client.send` Runtime/Network first, raw same-worker CDP fallback
- no `worker.evaluate()`
- post-worker browser liveness

## Block status and package rule

For every B01-B15 report:

- required
- executed
- passed
- failed
- blocked
- missing

A block is literal PASS only if every required assertion is `executed:true, pass:true`, with failed=blocked=missing=0.

If ANY B01-B15 is not literal PASS:

- B16 = `NOT_RUN`
- create NO ZIP
- emit `OZON_RESOLVED_PACKAGING_FORBIDDEN_NOT_ALL_BLOCKS_PASS`
- continue only to final report publication

Only if every B01-B15 is literal PASS may the hard packaging interlock run.

Then build a NEW ZIP from exactly the tested 17-file tree, fresh-extract, byte-verify every file, rerun JS syntax/manifest/inventory checks, record new SHA-256, and set B16 PASS only after all package checks PASS.

Only B01-B16 all literal PASS may emit:

`OZON_FULL_PRE_OPERATOR_HANDOFF_GATE_PASS`

## One final report only

Create one report branch from the exact commit containing this standalone prompt:

`validation/ozon-resolved-pre-operator-full-gate-2026-08-19`

Publish exactly one report-only commit/file. Do not commit temporary helper construction iterations or production artifacts to that branch.

The final report must contain everything required by the resolved execution contract, including the complete failure sets rather than first-error-only reporting.

After publication STOP and return exactly one result schema:

`OZON_PRE_OPERATOR_RESOLVED_FULL_GATE_RESULT`

with at minimum:

```text
verdict: PASS|FAILED
classification: NONE|PRODUCTION_BEHAVIOR_FAILURE|HARNESS_FIXTURE_FAILURE|HARNESS_ERROR|ENVIRONMENT_ERROR|SAFETY_ABORT|MULTIPLE
construction_audit: PASS|FAIL
construction_unresolved_defects: <count>
runner_sha256: <sha|NONE>
runner_execution_count: 0|1
candidate_reconstruction: PASS|FAIL|NOT_RUN
browser_substrate: PASS|FAIL|NOT_RUN
ledger_total_required: 164
ledger_executed: <n>
ledger_passed: <n>
ledger_failed: <n>
ledger_blocked: <n>
ledger_missing: <n>
block_01: PASS|FAIL|NOT_RUN
block_02: PASS|FAIL|NOT_RUN
block_03: PASS|FAIL|NOT_RUN
block_04: PASS|FAIL|NOT_RUN
block_05: PASS|FAIL|NOT_RUN
block_06: PASS|FAIL|NOT_RUN
block_07: PASS|FAIL|NOT_RUN
block_08: PASS|FAIL|NOT_RUN
block_09: PASS|FAIL|NOT_RUN
block_10: PASS|FAIL|NOT_RUN
block_11: PASS|FAIL|NOT_RUN
block_12: PASS|FAIL|NOT_RUN
block_13: PASS|FAIL|NOT_RUN
block_14: PASS|FAIL|NOT_RUN
block_15: PASS|FAIL|NOT_RUN
block_16: PASS|FAIL|NOT_RUN
failure_set_count: <n>
harness_defect_count: <n>
environment_defect_count: <n>
production_assertion_failure_count: <n>
blocked_assertion_count: <n>
real_ozon_requests: 0
real_performance_requests: 0
real_chatgpt_requests: 0
operator_browser_actions: 0
production_modifications: 0
candidate_drift: 0
source_cft_modifications: 0
packaging_interlock: PASS|FAIL|NOT_RUN
package_path: <path|NONE>
package_sha256: <sha|NONE>
fresh_extract_byte_identical: PASS|FAIL|NOT_RUN
umbrella_marker: PRESENT|ABSENT
report_branch: validation/ozon-resolved-pre-operator-full-gate-2026-08-19
report_commit: <sha>
```

This synthetic/Codex gate, even if PASS, is not logged-in live acceptance. The complete v0.1.19 logged-in live suite remains a separate later operator stage.
