# P4/P7 — продолжение после прерванного ответа

Дата: 2026-09-12. Статус: RESTORING_VERIFIED_WIP / NOT_A_RELEASE.

## Проверенная точка входа

Live WB ref прочитан заново: `3a9085b0f87f3b65ae51fed4b9cefd9d7dc65ae7`, ветка `stage06-wb-terminal-2026-09-08`, репозиторий `MaksimUnimax/blood_sand`.
Полностью прочитаны EXECUTION_CURSOR.json v9 (blob `6ad498993f4a1adfa405282a35980189641d9a1c`) и P1_POPUP_WORK_CHECKPOINT.md (blob `81317f971d34e165896fde339df5372ff48612f0`).

P1 popup, Work authority, собственная WB-кнопка и initial errors уже реализованы и опубликованы капсулами. Их не разрабатывать и не публиковать заново. Предыдущие 269 targeted assertions — исторический результат точного WIP, не новый прогон и не installed PASS.

В текущем mounted runtime найден точный WB021 ZIP и исходный Ozon ZIP; прежний каталог wb_parity_corrective_2026-09-12 содержит только сохранённое изображение, а не полный рабочий код. Поэтому восстановление выполнять из УЖЕ опубликованных капсул в установленном порядке, проверяя SHA-256. Не считать отсутствие локального кода потерей remote WIP.

## Цель следующего блока

P4/P7 Start остаётся OPEN_BLOCKS_HANDOFF: заменить мгновенный state flip на durable intent/revision -> durable commit перед единственным Send -> коррелированное подтверждение user turn либо unknown_no_retry -> новый завершённый assistant response -> active_visible. Восстановление не должно повторять Send/API.

Следующие действия: восстановить точные байты трёх сохранённых corrective-блоков; прочитать актуальную superseding authority и применимые Ozon patch rules; закрепить late Ozon source refs; сохранить causal comparison и RED до исправления. Далее реализовать и проверить связный P4/P7 блок. Не повторять инвентаризацию 171 ветки.

## Границы

Новых runtime-изменений в этой записи: 0; новых тестов: 0; marketplace provider calls: 0; бизнес-изменений: 0. Никаких новых owner-тестов/R1–R8. Не выдавать WIP как v0.2.2. Остальные открытые P2, HELP V2, P5–P9 сохраняются.

Сохранение — после смыслового блока/дефекта/группы тестов, при длительном чтении не позднее пяти содержательных read/search вызовов, перед долгим действием. Хранить код, diff, runner, stdout/stderr и результаты, а не только пересказ. После remote-записи проверять readback. Продолжение этой записи будет дополнено фактическими результатами, без переписывания старого evidence.
