# Block008 continuation — tool-output boundary

Scope: continue the authorized Phase1 A01/A02 text Multi-AI and conversation identity transfer. No new marketplace calls or business mutations are authorized by this checkpoint.

## Preserved source of truth

The previous execution transcript records completed block007:

- WB0.1.4 ZIP: 94498 bytes, SHA-256 `725c7e4bccb22d132b9e624f4acaa3a79a4f502635f3ff64da2b600124f32f8f`.
- Code/evidence commit: `a3e8cb80d0d60e57ac970a20537894f55b68565a`.
- Handoff receipt commit: `b8833bf4c203df775bfbc1bfc77684eddd6931c4`.
- Reported, saved scoped checks:313 behavioral and40 package. These are previous exact-target results, NOT newly rerun assertions in this continuation.
- Block008 scope-freeze commit: `454e62ab97c1bb852a03f894d00c0d4fbe4eafab`.
- Last prior update says the text adapter and identity code were connected and about to be saved before tests. That sentence alone does not establish a published code SHA.

Do not restart blocks001–007 or search again for a supposedly nonexistent0.1.4. Use the pinned block007 reconstructor if necessary. Do not overwrite existing block008 working files or invent the state of that candidate.

## Current boundary

This continuation attempted local filesystem reads, Python execution and a GitHub execution-cursor read. Their outputs were not available to the assistant for verification. Therefore no new candidate identity, test PASS, remote HEAD or successful write is asserted here. The creation of this document itself must be confirmed by subsequent readback before it is called a published checkpoint.

A bounded offline preservation/checking runner was authored and submitted for execution under `/mnt/data/WB_BLOCK008_CONTINUATION/`. The intended runner saves exact candidate sources before assertions, fsyncs each result, checks manifest references, JavaScript syntax, adapter host/mode selection in isolated VMs and unchanged WB provider files against the pinned0.1.4 ZIP. Candidate selection must be unambiguous; no missing source is replaced with a fabricated build. It uses no marketplace credentials or network. VM adapter checks are not browser, Send or complete identity certification.

The execution output was not available, so this document deliberately does not claim that the runner completed, that its output files exist, or that any assertion passed.

## Resume precisely

1. Read the current remote execution cursor and block008 files, preserving any newer work.
2. Locate actual block008 candidate and existing test recipes; identify exact file hashes and code diff.
3. Save actual code/diff/tests in a WIP checkpoint before long tests. Do not save only another narrative.
4. Check text adapters and conversation identity through all real consumers: content capture, Manual, Autorun, worker sender URL verification, binding keys, storage restoration, legacy ChatGPT-only migration, reinitialization and Send ownership.
5. Test cross-AI and cross-conversation isolation, unknown-host rejection, same-provider positive controls, stale generation rejection, Alice disabled/blocked Send and text delivery. Do not mark file attachment support complete in this text-only block.
6. Run relevant prior regression suites against the exact resulting artifact, then persist raw logs, runner hashes, candidate hashes, cursor and remote readback.

Full Phase1 migration and live certification remain incomplete. An unavailable response must not be converted into a product FAIL or a test PASS.
