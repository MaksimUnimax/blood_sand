# Block008 — Multi-AI text adapter and identity integration

2026-09-11. Authorized continuation: «Продолжай так же сохраняя промежуточный прогресс».

Base: verified WB0.1.4 WIP, 94498 bytes, SHA256 `725c7e4bccb22d132b9e624f4acaa3a79a4f502635f3ff64da2b600124f32f8f`. Previous final publication `a3e8cb80d0d60e57ac970a20537894f55b68565a`, handoff receipt `b8833bf4c203df775bfbc1bfc77684eddd6931c4`. Blocks001–007 are complete within their explicit scope and are not restarted.

## Scope and donor

A01/A02 bounded slice: ChatGPT/Alice text discovery, composer/send adapter, confirmed conversation identity and binding. Donor is the operator's Ozon ZIP `OZON_BRIDGE_v0.1.19_MULTI_AI_FILE_DELIVERY_5aa1b4a21a0a.zip`, SHA256 `a5765f11148b921baeff5e18b64b428c9d598f9fbb41b8ff91fd9cef7cb8fb83`. Extracted 30 files exist locally. Do not equate this ZIP to every later Ozon commit.

Use provider-neutral identity/composer patterns, adapt text-only AI surfaces. No new WB endpoints, auth, quota numbers, cache, privacy permissions, subscription rules or provider acquisition. Files/attachments, Work Session and remaining A-features remain separate pending blocks. Do not advertise attachment capability merely because the donor contains file helpers.

## Dependency inventory to close

1. Exact HTTPS origin -> AI identity -> URL/path/canonical or Alice active-history corroboration -> content identity -> worker normalization -> actual tab verification -> binding key/revision -> manual/auto ownership -> delivery/recovery -> popup labels and diagnostics.
2. Legacy `chatgpt:c:<UUID>` migration must apply to ChatGPT only, never to Alice with the same UUID.
3. Assistant/user DOM adapters -> chronological turn order -> stable completed-source capture -> shared HELP/API parser -> Manual selected block / Autorun whole assistant -> existing worker pipeline. Raw command strings must preserve NBSP.
4. Composer root/form -> sender button classification -> captured owner -> commit-before-send -> exact text write/send -> stale runtime guards. Alice is not assumed to have an HTML form.
5. Custom Copy adapter IDs and Send profiles -> storage -> get/save/clear -> broadcast -> content settings -> popup controls. A ChatGPT Send profile must not cross into Alice.
6. Manifest load order/host permissions -> actual package modules -> previous ChatGPT regression plus independent synthetic Alice tests.

## Implementation and evidence rule

Save code/recipes and hashes before the next long group. Existing 0.1.4 ZIP remains immutable. If a test needs an actual HTTPS origin, adapt only the synthetic test fixture URL; do not permit unknown origins in production to satisfy an about:blank mock. Keep all failures and timeout receipts. No PASS from source presence or old counts.

Expected test layers: deterministic identity and worker/serialization; real Chromium on synthetic ChatGPT/Alice pages with provider traffic blocked; regression of previous ingress/owner/batch functions; exact next-version ZIP. Genuine installed-profile checks remain PENDING_POST_INSTALL. This plan is not an implementation claim or feature acceptance.
