{
  "name": "wildberries-llm-api-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {"test": "node --test tests/*.test.mjs", "test:coverage": "node --experimental-test-coverage --test tests/*.test.mjs"},
  "description": "Read-only ChatGPT to Wildberries API bridge with exactly-once lifecycle and fixed operation registry."
}
