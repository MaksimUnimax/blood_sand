MARKETPLACE QUESTION OPERATOR — R6B CODEX RUNTIME COMPLETION

Project: /opt/marketplace-question-operator
Current HEAD: 695da204c7bab3342f5f7f0949369416b64a71c0

Continue implementation from the current codebase. This is a coding task, not an audit.

Immediately implement the remaining Codex runtime integration and keep iterating until the requested behavior is present and the test suite passes. Do not stop merely because several connected changes are required. Only stop without code changes for a concrete environment/tool failure; if that happens, report the exact failing command and exact error.

Already implemented and to be preserved:
- single composite prompt path;
- Codex attempt creation/profile capture;
- guarded CODEX_RUNNING transition;
- success/error persistence primitives;
- source=codex immutable revision creation;
- REVIEW completion on successful fake generation;
- no marketplace publish from the Codex success path.

Implement the remaining connected Codex runtime in this run:

1. PRODUCTION LOCAL CODEX RUNNER

Use the installed binary:
/root/.nvm/versions/node/v22.22.1/bin/codex

Profiles:
codex1 -> /root/.codex
codex2 -> /root/.codex_second
codex3 -> /root/.codex_third

Implement the runner with asyncio.create_subprocess_exec, never shell=True.

Each attempt gets its own job directory under the configured jobs root.

Invoke Codex using the valid exec form supported by the installed CLI, with:
- explicit working/job directory;
- --json;
- --ephemeral;
- workspace-write sandbox configuration;
- explicit captured CODEX_HOME.

Build the child environment deliberately. Set HOME=/root, CODEX_HOME=<captured profile>, PATH and required locale values. Do not pass TELEGRAM_BOT_TOKEN, TELEGRAM_OPERATOR_USER_ID, OZON_CLIENT_ID, OZON_API_KEY or WB_API_TOKEN into the child environment.

Tests must inject a fake subprocess/runner; do not make a real model call.

2. JSONL FINAL ANSWER EXTRACTION

Implement deterministic parsing of codex exec --json JSONL.

Extract only the final assistant answer intended to become the customer draft. Do not concatenate reasoning, tool events, diagnostics or intermediate messages.

Handle empty, malformed and incomplete output safely.

Classify failures at least as:
LIMIT
AUTH
TIMEOUT
NONZERO_EXIT
INVALID_OUTPUT
PROCESS_ERROR

Prefer a stronger recognized LIMIT/AUTH classification over generic NONZERO_EXIT when the emitted output clearly supports it.

3. STALE ATTEMPT PROTECTION

Before applying either success or error completion, verify the completed attempt is still questions.current_draft_attempt_id for that question and that the question is still in the expected CODEX_RUNNING lifecycle for that attempt.

A late result from an obsolete attempt must not:
- create a revision;
- replace current_answer_revision_id;
- overwrite current_draft_attempt_id;
- move question state;
- replace a newer attempt result.

4. TELEGRAM CODEX CALLBACK FLOW

Wire the production callback/service flow for:
- codex
- regenerate
- retry_codex
- choose_codex

NEW -> explicit Codex:
- capture active profile;
- create one attempt;
- set current attempt;
- transition NEW -> CODEX_RUNNING;
- launch generation asynchronously.

REVIEW -> Regenerate:
- preserve old revisions/attempts;
- create a new attempt using the active profile at that moment;
- transition REVIEW -> CODEX_RUNNING;
- launch generation.

CODEX_ERROR -> Retry:
- create a new attempt for the same question/Q-ID;
- capture the active profile at retry time;
- transition CODEX_ERROR -> CODEX_RUNNING;
- launch generation.

Changing profile only changes active_codex_profile. It performs zero generation and creates zero attempts by itself.

Duplicate Codex taps while a question is CODEX_RUNNING must create at most one active attempt/subprocess.

5. SUCCESS / ERROR CARDS

On success, render/update a Telegram review card containing:
- Q-ID;
- marketplace;
- original buyer question;
- exact Codex draft;
- Generated by: <captured attempt profile>;
- Currently active: <current profile>.

Reuse existing Send/Edit/Regenerate/Ignore controls.

On failure, render/update an error card containing:
- Q-ID;
- marketplace;
- original question;
- failed profile;
- classified error;
- currently active profile.

Actions:
- Retry;
- Change Codex;
- Answer manually;
- Ignore.

Manual fallback from CODEX_ERROR must reuse the existing manual Reply-correlation workflow for the same question. Ignore remains local only.

6. REQUIRED BEHAVIORAL TESTS

Use temp SQLite, fake Telegram transport/handler objects and fake subprocess/runner fixtures. No public network and no real Codex execution.

Cover at minimum:
- runner uses captured CODEX_HOME;
- runner uses asyncio subprocess API with no shell=True;
- child env excludes all marketplace/Telegram secrets;
- each attempt gets a unique job directory;
- valid representative JSONL returns exact final assistant draft only;
- malformed/empty JSONL -> INVALID_OUTPUT;
- timeout -> TIMEOUT;
- nonzero exit -> NONZERO_EXIT unless LIMIT/AUTH is positively identified;
- NEW -> Codex creates exactly one attempt and CODEX_RUNNING;
- duplicate tap during CODEX_RUNNING creates no second attempt/subprocess;
- success creates immutable source=codex revision and REVIEW;
- stale old attempt success cannot overwrite a newer attempt/revision/state;
- stale old attempt failure cannot overwrite a newer attempt/state;
- generation failure -> CODEX_ERROR with persisted error;
- profile switch alone performs zero generations/attempts;
- explicit retry from CODEX_ERROR uses the newly active profile and same question/Q-ID;
- regenerate from REVIEW creates a new attempt and preserves old revision;
- success card shows generated-by and current-active separately;
- manual fallback from CODEX_ERROR enters existing manual workflow;
- no Codex success path invokes marketplace send automatically.

Add a fake end-to-end flow:
question -> explicit Codex codex1 -> fake LIMIT -> CODEX_ERROR -> switch active to codex2 -> no automatic retry -> explicit Retry -> new attempt codex2 -> fake valid JSONL -> REVIEW.

Also add a stale-race E2E proving a late attempt result cannot replace the currently active attempt/result.

Run focused tests while implementing, then:

.venv/bin/python -m pytest -q

All existing tests must remain passing.

No live Telegram/Ozon/Wildberries requests. No real Codex model request. No credentials requested or stored. No systemd changes. Do not modify /root/.codex, /root/.codex_second or /root/.codex_third contents. Do not touch unrelated services/projects.

After implementation, inspect git status/diff, verify no secrets/auth material are tracked, and commit:

feat: complete Codex runtime integration

Return a concise summary with START_HEAD, FINAL_HEAD, files changed, test command/result/count, and anything genuinely still missing before controlled credential/live-read setup.