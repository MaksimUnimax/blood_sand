MARKETPLACE QUESTION OPERATOR — R8 RUNTIME COMPLETION + DEPLOYMENT PREPARATION

Project: /opt/marketplace-question-operator
Current HEAD: 793f925a59defec357ddd1783ac8cca71075265c

This is a coding task. Do not audit the project and do not assess overall readiness before editing code.

Implement ALL items in this ticket in the same run. Keep a private todo list and continue coding/testing after each completed item. Do not stop after the first few changes. Task size is not a blocker. Only stop without code changes for a concrete command/tool/environment error; if that happens, report the exact failing command and exact error.

Do NOT perform live Telegram/Ozon/Wildberries calls and do NOT ask for real secrets in this task. This run finishes the remaining offline runtime and creates deployment artifacts so the next run can be controlled live bring-up.

The existing code already includes marketplace adapters, persistence/state primitives, Telegram authorization/basic handlers, manual/edit/send orchestration, Codex composite prompt, Codex runner JSONL parsing/job directories/secret-free env, stale-attempt protection, runner timeout cleanup, and persisted Codex profile switching. Preserve and extend the existing implementation.

Implement the following connected areas.

1. COMPLETE TELEGRAM CODEX CALLBACK FLOW

Wire production callbacks for:
- codex
- regenerate
- retry_codex
- choose_codex

Required behavior:

NEW + codex:
- capture current active Codex profile;
- create exactly one draft attempt for the same question;
- set it as current;
- transition NEW -> CODEX_RUNNING;
- invoke the runner abstraction asynchronously.

REVIEW + regenerate:
- preserve prior revisions and attempts;
- capture active profile at regeneration time;
- create a new attempt for the same question/Q-ID;
- set it current;
- transition REVIEW -> CODEX_RUNNING;
- invoke the runner.

CODEX_ERROR + retry_codex:
- create a new attempt for the same question/Q-ID;
- capture active profile at retry time;
- transition CODEX_ERROR -> CODEX_RUNNING;
- invoke the runner.

choose_codex:
- persist codex1/codex2/codex3 through existing validation;
- zero generation;
- zero new attempts.

Duplicate codex/regenerate/retry taps while CODEX_RUNNING must create at most one active attempt and one runner invocation.
Malformed/stale callbacks must launch zero runner invocations.

2. COMPLETE CODEX SUCCESS / ERROR TELEGRAM CARDS

Success review card must show:
- Q-ID
- marketplace
- original buyer question
- exact Codex draft
- Generated by: captured attempt profile
- Currently active: current global profile

Use existing Review actions:
- Send
- Edit
- Regenerate
- Ignore

Error card must show:
- Q-ID
- marketplace
- original buyer question
- failed attempt profile
- classified error type/message
- currently active profile

Actions:
- Retry
- Change Codex
- Answer manually
- Ignore

Changing profile performs no retry by itself.
Manual fallback from CODEX_ERROR must reuse the existing Reply-linked manual workflow for the same question.
Ignore is local only.
Buyer/operator text remains plain untrusted text and never appears inside callback_data.

3. FINISH FAKE CALLBACK / RACE / SUBPROCESS COVERAGE

Add behavioral tests around production service/handler/runner code for at least:
- NEW -> codex creates one attempt + CODEX_RUNNING + one runner invocation;
- duplicate codex tap while CODEX_RUNNING creates no second attempt/run;
- REVIEW -> regenerate creates a new attempt and preserves old revision;
- CODEX_ERROR -> choose_codex changes profile with zero attempt/run;
- explicit retry after profile switch uses the new profile and same question/Q-ID;
- success creates source=codex revision and renders generated-by/current-active separately;
- failure renders the Codex error card;
- manual fallback from CODEX_ERROR enters existing Reply workflow;
- stale/malformed Codex callbacks start zero runners;
- late stale success cannot overwrite current attempt/revision/state;
- late stale failure cannot move current state;
- runner timeout kills/awaits child and returns TIMEOUT;
- no Codex outcome path publishes to a marketplace automatically.

Add one integrated fake flow:
question -> codex1 -> fake LIMIT -> CODEX_ERROR -> choose codex2 -> verify zero automatic retry -> explicit Retry -> new attempt codex2 -> fake success -> REVIEW.

4. COMPLETE TELEGRAM RENDERING GAPS

Ensure production rendering exists for these operational states:
- NEW
- MANUAL_INPUT
- EDITING
- REVIEW
- CODEX_RUNNING
- CODEX_ERROR
- SEND_FAILED
- SEND_UNKNOWN
- SENT

Every important card keeps Q-ID, marketplace and original question, plus current answer/draft/delivery context when relevant.
Do not silently truncate long content; split deterministically into continuation messages retaining Q-ID/context.
Only TELEGRAM_OPERATOR_USER_ID may receive buyer/answer/operational question data.

5. CREATE RUNNABLE APPLICATION ENTRY POINT

Create or complete the real daemon entry point that wires:
- configuration loading;
- SQLite repository;
- Ozon adapter;
- Wildberries adapter;
- Telegram application;
- MarketplaceQuestionService/orchestrator;
- Codex runner;
- 600-second marketplace polling loop;
- daily/periodic retention cleanup.

Use these frozen paths by default:
PROJECT=/opt/marketplace-question-operator
STATE_DB=/var/lib/marketplace-question-operator/state.sqlite3
JOBS_DIR=/var/lib/marketplace-question-operator/jobs
SECRETS_FILE=/etc/marketplace-question-operator/secrets.env

The entry point must support graceful startup/shutdown and clear non-secret configuration errors.
Do not print secret values.

6. CONFIGURATION + SECRET FILE CONTRACT

Production configuration must load exactly these secret values from environment/secrets.env when live mode is later used:
- TELEGRAM_BOT_TOKEN
- TELEGRAM_OPERATOR_USER_ID
- WB_API_TOKEN
- OZON_CLIENT_ID
- OZON_API_KEY

Do not create a real secrets.env in this task.
Provide an example/template file containing only variable names/placeholders, safe to commit.
Ensure the real /etc/.../secrets.env path is git-ignored/not inside the repo.

Add configuration tests proving:
- missing required production values fail clearly;
- errors do not reveal provided secret values;
- test/offline dependency injection remains usable without live credentials.

7. DEPLOYMENT ARTIFACTS

Create minimal deployment artifacts for one Python daemon:
- systemd unit template/file;
- install/deploy helper that creates /var/lib/marketplace-question-operator and jobs directory with safe ownership/permissions;
- documentation or executable helper for installing the unit and starting/restarting later;
- secure secret installer script/helper that WILL be used in the next run, but DO NOT run it now and DO NOT ask for secrets now.

The future secret installer must write:
/etc/marketplace-question-operator/secrets.env
with root ownership and mode 0600 (or stricter), without echoing secret values after entry.

Systemd service should:
- use the project virtualenv Python;
- set WorkingDirectory=/opt/marketplace-question-operator;
- load EnvironmentFile=/etc/marketplace-question-operator/secrets.env;
- restart on failure;
- run the single application daemon entry point;
- not alter nginx/Docker/PostgreSQL or unrelated services.

Do not install/enable/start the unit in this task.

8. POLLING RUNTIME FINALIZATION

Ensure the daemon polling loop:
- polls Ozon and WB independently;
- uses 600-second nominal cadence;
- prevents overlap for the same marketplace;
- one marketplace failure does not stop the other;
- duplicate questions do not duplicate Telegram cards;
- shutdown cancels polling cleanly.

Add behavioral tests with fake adapters/clock/tasks where practical.

9. VALIDATION

Run focused tests while implementing, then run the full suite:

.venv/bin/python -m pytest -q

All existing tests must remain passing.

Also run non-live checks for the new entry/deployment artifacts, for example Python import/config validation and systemd unit syntax-oriented inspection if available without altering the system.

No public network.
No live Telegram.
No live Ozon/WB.
No real Codex model generation.
No real secrets.
No systemctl enable/start.
Do not modify /root/.codex, /root/.codex_second, or /root/.codex_third contents.
Do not touch unrelated services/projects.

10. FINISH

Inspect git status and git diff --stat. Verify no credentials/auth material are tracked.

Commit ALL implementation from this ticket. Multiple commits are acceptable if useful, but do not stop after the first commit; continue until all ticket items are implemented and tests pass.

Suggested commit messages:
- feat: finish Codex Telegram workflow
- feat: add production daemon and deployment artifacts

Return a concise summary with:
START_HEAD
FINAL_HEAD
commits created
files changed
full test command/result/count
remaining concrete work before controlled live bring-up

Do not return a readiness assessment instead of implementation work. The expected remaining work after this ticket is only controlled secret installation, live read-only Telegram/Ozon/WB smoke checks, systemd start, and later explicit marketplace-send acceptance.