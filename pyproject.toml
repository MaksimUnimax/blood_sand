[project]
name = "blood-sand-recommendations"
version = "0.0.0"
description = "Locked dependency baseline for the recommendations service"
requires-python = ">=3.11"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.52.4",
    "pydantic==2.13.5",
    "httpx==0.28.1",
    "dateparser==1.4.2",
]
