# Direct browser SERP observation — `алатырь оберег`

Observed: 2026-08-26; exact clock time not independently captured  
Surface: Yandex Search desktop browser UI  
Evidence mode: direct user-provided copied SERP text  
Exact query: `алатырь оберег`  
Device: DESKTOP

## Provenance / localization

Copied Search URLs contain `lr=11202`. This numeric browser localization parameter is directly observed. Its human-readable geography is not resolved here and must not be silently relabeled as Search API region `225`.

## Directly observed SERP composition

The copied browser page directly contains:

- embedded `Быстрый ответ Алисы AI` for the exact query;
- Alice image thumbnails;
- marketplace/shopping results from `wildberries.ru`, `ozon.ru`, `livemaster.ru`, `market.yandex.ru`;
- specialist commerce/content results including `slavyanskieoberegi.ru`, `ruyan-master.ru`, `master-run.ru`, `simvolroda.ru`, `veseliyviking.ru`, `logovo-volka.ru`;
- editorial/informational result `legenti.ru`;
- social result `vk.ru`;
- dedicated `Видео` carousel;
- multiple additional `Промо` results farther down the copied page;
- dedicated `Картинки` block with 13 visible image entries plus `Все картинки`;
- direct `Люди ищут` block with 10 related queries.

Because this evidence is copied full-page text rather than an above-the-fold screenshot, exact visual viewport placement is not inferred beyond the preserved labels/order.

## `Люди ищут` — direct UI observation

1. `алатырь оберег значение`
2. `алатырь оберег значение для мужчин`
3. `алатырь оберег фото`
4. `алатырь оберег это`
5. `алатырь оберег значение для женщин фото и описание`
6. `алатырь оберег картинки`
7. `алатырь оберег что значит`
8. `алатырь оберег значение для женщин`
9. `алатырь оберег купить серебро мужской`
10. `алатырь оберег вышивка`

This is direct evidence of strong meaning/explanation and gender-specific interpretation branches alongside an explicit transactional branch (`купить серебро мужской`).

## Video block

A dedicated `Видео` block is directly present. The copied entries include Dzen, Rutube, VK Video and Mail.ru origins, with titles focused on `значение`, `свойства`, `описание`, handmade products and symbolic interpretation.

## Image block

The dedicated `Картинки` block includes 13 visible entries. Visible image origins include Yandex marketplace image storage, Livemaster and specialist jewelry/product sites. This is recorded as a visual-discovery surface, not ordinary organic ranking.

## Embedded Alice

The embedded answer explicitly displays 18 sources. The captured answer text is partial because the copied block ends during the `Как использовали оберег` section, but the 18-source list is complete in the supplied page copy.

Stored separately in:

`marketing/data/raw/alice/20260826__alatyr_obereg__embedded.md`

## Evidence-safe interpretation

- Search is strongly commercial while simultaneously exposing an explanatory Alice answer.
- Direct related-query evidence shows that `значение`, gender-specific meaning and imagery are important query branches.
- Specialized commercial/content sites appear in both ordinary Search and Alice's source set.
- No final Page Job or IA decision is assigned at this stage.
