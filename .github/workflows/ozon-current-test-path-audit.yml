name: Ozon current test path audit

on:
  push:
    branches:
      - dev/ozon-v0.1.19-manual-delivery-composer-wait-2026-08-18
    paths:
      - '.github/workflows/ozon-current-test-path-audit.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ozon-current-test-path-audit
  cancel-in-progress: false

env:
  FROZEN_ZIP: tooling/llm-api-bridges/ozon-seller/artifacts/OZON_BRIDGE_v0.1.19_TESTED_FROZEN_REPAIR_66bc4ac.zip
  FROZEN_ZIP_SHA256: d794e9fe8550dcf20d76d63abf7832d9b28853ad6d7c8e94faa22a3c08a46a2c
  PATCH_0_BLOB: 4b4578995156cafd60221f8d57f678b99b0b00ff
  PATCH_1_BLOB: 98feec99e459332df60aae879fa8f2530856c2d0
  PATCH_SHA256: bd0119212a21c63fbbc2a6d0067c0c19abcf31896007e8f8d3e19efa8255019d
  FINAL_WORKER_SHA256: dfc101f6d1840af89b7dc48b6082f43b26c8143ed96086bc19ab0dfd36c21fac
  FINAL_CONTENT_SHA256: ab3408a2637153fa324f0b679ac5452b9b7ae0182f5ccf4f0397ccd960857dda
  READY_TEST_SOURCE_COMMIT: 5049fe6d579b41f94069d7f2d2a8d3f5d1198c62

jobs:
  vm-paths:
    name: Reconstruct + execute non-browser paths
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact branch history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup exact Node
        uses: actions/setup-node@v4
        with:
          node-version: '24.12.0'

      - name: Reconstruct exact current candidate
        id: reconstruct
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          WORK="$RUNNER_TEMP/ozon-path-audit"
          CANDIDATE="$WORK/candidate"
          PATCH="$WORK/composer-wait.patch"
          rm -rf "$WORK"
          mkdir -p "$CANDIDATE"

          echo "$FROZEN_ZIP_SHA256  $FROZEN_ZIP" | sha256sum -c -
          git cat-file blob "$PATCH_0_BLOB" > "$WORK/00.patch.part"
          git cat-file blob "$PATCH_1_BLOB" > "$WORK/01.patch.part"
          cat "$WORK/00.patch.part" "$WORK/01.patch.part" > "$PATCH"
          test "$(wc -c < "$PATCH")" -eq 13648
          echo "$PATCH_SHA256  $PATCH" | sha256sum -c -
          unzip -q "$FROZEN_ZIP" -d "$CANDIDATE"

          cd "$CANDIDATE"
          git init -q
          git config core.autocrlf false
          git apply --check "$PATCH"
          git apply "$PATCH"
          echo "$FINAL_WORKER_SHA256  service_worker.js" | sha256sum -c -
          echo "$FINAL_CONTENT_SHA256  content_script.js" | sha256sum -c -
          find . -type f ! -path './.git/*' -printf '%P\n' | sort > "$WORK/inventory.txt"
          test "$(wc -l < "$WORK/inventory.txt")" -eq 17
          echo "CANDIDATE=$CANDIDATE" >> "$GITHUB_ENV"
          echo 'CURRENT_CANDIDATE_RECONSTRUCTION_PASS'

      - name: Materialize and repair only proven harness-fixture defects
        id: materialize
        if: steps.reconstruct.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set -euo pipefail
          TESTDIR="$RUNNER_TEMP/ozon-path-audit/tests"
          mkdir -p "$TESTDIR"
          git fetch --no-tags origin "$READY_TEST_SOURCE_COMMIT"
          declare -A BLOBS=(
            [B02_B03_CONTRACT_CURRENT.mjs]=b1a79c8eaf115751fa2895e470434c3a06754a7b
            [B04_CAPABILITY_CURRENT.mjs]=f4cd9ca231a534228a27e913a23fba66f795ed60
            [B05_B07_B08_ANALYTICS_CURRENT.mjs]=76cd3a02123636eb60e5e048767a26d0178b49f1
            [B06_WORKER_QUOTA_CURRENT.mjs]=07017368b33979169e7b32328280cb8d25e19c3d
            [B09_COMMON_BATCH_CURRENT.mjs]=19ed8d8779f72325b7034e746fc8746137592469
            [B11_B12_COMPOSER_WAIT_CURRENT.mjs]=77e99abe10b1468cefff6f0f4ae4b471ef47f7a6
            [B14_PERFORMANCE_CURRENT.mjs]=e9bd146e0f98000d31912a6b4412e87c44cf6285
          )
          for name in "${!BLOBS[@]}"; do git cat-file blob "${BLOBS[$name]}" > "$TESTDIR/$name"; done

          python3 - "$TESTDIR" <<'PY'
          from pathlib import Path
          import sys
          root=Path(sys.argv[1])

          def replace(name, old, new):
              p=root/name
              s=p.read_text(encoding='utf-8')
              if old not in s:
                  raise SystemExit(f'missing expected fixture text in {name}: {old[:100]}')
              p.write_text(s.replace(old,new),encoding='utf-8',newline='\n')

          # B02: posting_fbs_get is a PII-boundary execution prohibition, not necessarily a parse prohibition.
          replace('B02_B03_CONTRACT_CURRENT.mjs',
          "for(const operation of ['seller_info','posting_fbs_get','posting_fbs_cancel','order_cancel','arbitrary_http']){\n  expectReject({operation,params:{}},`blocked/unsupported operation ${operation}`);\n}\nconsole.log('B02_INTERNAL_AND_BLOCKED_OPERATIONS_PASS');",
          "for(const operation of ['seller_info','posting_fbs_cancel','order_cancel','arbitrary_http']){\n  expectReject({operation,params:{}},`blocked/unsupported operation ${operation}`);\n}\nconst piiCommand=expectAccept({operation:'posting_fbs_get',params:{}},'posting_fbs_get parse boundary');\nlet piiBlocked=false;\ntry{const pre=C.preflightExecution(piiCommand);piiBlocked=pre?.blocked===true||pre?.ok===false||pre?.execution_allowed===false;}catch(_){piiBlocked=true;}\nassert(piiBlocked,'posting_fbs_get was not blocked before provider execution');\nconsole.log('B02_POSTING_FBS_GET_EXECUTION_BLOCKED_PASS');\nconsole.log('B02_INTERNAL_AND_BLOCKED_OPERATIONS_PASS');")

          # B04/B05: previous fixtures passed host-realm objects into contract functions that require VM-realm JSON values.
          replace('B04_CAPABILITY_CURRENT.mjs',
          "const base={date_from:'2026-08-17',date_to:'2026-08-17',dimension:['day'],limit:10};\nconst universal=C.normalizeCommand({operation:'analytics_data',params:{...base,metrics:['revenue']}});\nconst restricted=C.normalizeCommand({operation:'analytics_data',params:{...base,metrics:['returns']}});\nconst mixed=C.normalizeCommand({operation:'analytics_data',params:{...base,metrics:['revenue','returns']}});\nconst restrictedDimension=C.normalizeCommand({operation:'analytics_data',params:{...base,dimension:['brand'],metrics:['revenue']}});",
          "const base={date_from:'2026-08-17',date_to:'2026-08-17',dimension:['day'],limit:10};\nconst realm=o=>vm.runInContext(`(${JSON.stringify(o)})`,context);\nconst currentCommand=o=>C.parseCommand(`OZON_API_V1\\n${JSON.stringify(o)}`);\nconst universal=currentCommand({operation:'analytics_data',params:{...base,metrics:['revenue']}});\nconst restricted=currentCommand({operation:'analytics_data',params:{...base,metrics:['returns']}});\nconst mixed=currentCommand({operation:'analytics_data',params:{...base,metrics:['revenue','returns']}});\nconst restrictedDimension=currentCommand({operation:'analytics_data',params:{...base,dimension:['brand'],metrics:['revenue']}});")
          replace('B04_CAPABILITY_CURRENT.mjs',
          "const premiumPlan=C.planCommandForSellerCapability(restricted,{status:'known',subscription_type:'PREMIUM_PLUS',is_premium:true,probe_performed:true},Date.now());assert(premiumPlan.action==='execute'&&premiumPlan.command.params.metrics.includes('returns'),'Premium Plus restricted analytics rejected');const unknownPlan=C.planCommandForSellerCapability(restricted,{status:'unknown',subscription_type:'UNKNOWN',probe_performed:true},Date.now());",
          "const premiumPlan=C.planCommandForSellerCapability(restricted,realm({status:'known',subscription_type:'PREMIUM_PLUS',is_premium:true,probe_performed:true}),Date.now());assert(premiumPlan.action==='execute'&&premiumPlan.command.params.metrics.includes('returns'),'Premium Plus restricted analytics rejected');const unknownPlan=C.planCommandForSellerCapability(restricted,realm({status:'unknown',subscription_type:'UNKNOWN',probe_performed:true}),Date.now());")

          replace('B05_B07_B08_ANALYTICS_CURRENT.mjs',
          "const cmd=(metrics=['revenue'],extra={})=>C.normalizeCommand({operation:'analytics_data',params:{date_from:'2026-08-17',date_to:'2026-08-17',dimension:['day'],metrics,limit:10,...extra}});",
          "const realm=o=>vm.runInContext(`(${JSON.stringify(o)})`,context);\nconst cmd=(metrics=['revenue'],extra={})=>C.parseCommand(`OZON_API_V1\\n${JSON.stringify({operation:'analytics_data',params:{date_from:'2026-08-17',date_to:'2026-08-17',dimension:['day'],metrics,limit:10,...extra}})}`);")
          replace('B05_B07_B08_ANALYTICS_CURRENT.mjs',
          "const providerRaw={result:{data:[{dimensions:[{id:'2026-08-17',name:'2026-08-17'}],metrics:[100,7]}],totals:[100,7]}};",
          "const providerRaw=realm({result:{data:[{dimensions:[{id:'2026-08-17',name:'2026-08-17'}],metrics:[100,7]}],totals:[100,7]}});")
          replace('B05_B07_B08_ANALYTICS_CURRENT.mjs',
          "const creds={clientId:'SELLER-CLIENT-123',apiKey:'SELLER-API-KEY-SECRET'};\nconst rotated={clientId:'SELLER-CLIENT-123',apiKey:'ROTATED-SELLER-API-KEY'};\nconst other={clientId:'OTHER-SELLER-999',apiKey:'OTHER-KEY'};",
          "const creds=realm({clientId:'SELLER-CLIENT-123',apiKey:'SELLER-API-KEY-SECRET'});\nconst rotated=realm({clientId:'SELLER-CLIENT-123',apiKey:'ROTATED-SELLER-API-KEY'});\nconst other=realm({clientId:'OTHER-SELLER-999',apiKey:'OTHER-KEY'});")

          # B06/B09: COMPOSER_NOT_FOUND was an invented mock response that stopped the actual worker before provider-path evidence.
          for name in ['B06_WORKER_QUOTA_CURRENT.mjs','B09_COMMON_BATCH_CURRENT.mjs']:
              replace(name,
              "const response=msg?.type==='OZ_GET_IDENTITY'?{ok:true,identity:clone(tabIdentities.get(id))}:{ok:false,code:'COMPOSER_NOT_FOUND'};",
              "const response=msg?.type==='OZ_GET_IDENTITY'?{ok:true,identity:clone(tabIdentities.get(id))}:{ok:true};")

          # B14: current credential helper uses camelCase trusted fields, not storage-key names.
          replace('B14_PERFORMANCE_CURRENT.mjs',
          "const perfCreds=Cred.normalizePerformanceCredentials({client_id:'PERF_CLIENT_ID',client_secret:'PERF_CLIENT_SECRET'},{required:true});",
          "const perfCreds=Cred.normalizePerformanceCredentials({clientId:'PERF_CLIENT_ID',clientSecret:'PERF_CLIENT_SECRET'},{required:true});")
          PY

          for name in "${!BLOBS[@]}"; do node --check "$TESTDIR/$name"; done
          echo "TESTDIR=$TESTDIR" >> "$GITHUB_ENV"
          echo 'CURRENT_TEST_SOURCES_SYNTAX_PASS'

      - name: Execute every non-browser path and collect complete result set
        id: execute
        if: steps.reconstruct.outcome == 'success' && steps.materialize.outcome == 'success'
        continue-on-error: true
        shell: bash
        run: |
          set +e
          LOG="$RUNNER_TEMP/vm-execution.log"
          : > "$LOG"
          failures=0
          run_one() {
            name="$1"
            echo "===== $name =====" | tee -a "$LOG"
            node "$TESTDIR/$name" "$CANDIDATE" 2>&1 | tee -a "$LOG"
            rc=${PIPESTATUS[0]}
            echo "EXIT_CODE[$name]=$rc" | tee -a "$LOG"
            if [ "$rc" -ne 0 ]; then failures=$((failures+1)); fi
          }
          run_one B02_B03_CONTRACT_CURRENT.mjs
          run_one B04_CAPABILITY_CURRENT.mjs
          run_one B05_B07_B08_ANALYTICS_CURRENT.mjs
          run_one B06_WORKER_QUOTA_CURRENT.mjs
          run_one B09_COMMON_BATCH_CURRENT.mjs
          run_one B11_B12_COMPOSER_WAIT_CURRENT.mjs
          run_one B14_PERFORMANCE_CURRENT.mjs
          echo "NON_BROWSER_FAILURE_COUNT=$failures" | tee -a "$LOG"
          if [ "$failures" -eq 0 ]; then
            echo 'CURRENT_NON_BROWSER_PATHS_EXECUTE_PASS' | tee -a "$LOG"
            exit 0
          fi
          echo 'CURRENT_NON_BROWSER_PATHS_EXECUTE_FAIL' | tee -a "$LOG"
          exit 1

      - name: Build non-browser evidence artifact
        if: always()
        shell: bash
        run: |
          OUT="$RUNNER_TEMP/vm-result.txt"
          {
            echo "trigger_sha=$GITHUB_SHA"
            echo "node=$(node --version 2>/dev/null || true)"
            echo "reconstruct=${{ steps.reconstruct.outcome }}"
            echo "materialize=${{ steps.materialize.outcome }}"
            echo "execute=${{ steps.execute.outcome }}"
            echo "frozen_zip_sha256=$FROZEN_ZIP_SHA256"
            echo "patch_sha256=$PATCH_SHA256"
            echo "final_worker_sha256=$FINAL_WORKER_SHA256"
            echo "final_content_sha256=$FINAL_CONTENT_SHA256"
            echo
            if [ -f "$RUNNER_TEMP/vm-execution.log" ]; then cat "$RUNNER_TEMP/vm-execution.log"; fi
          } > "$OUT"
          cat "$OUT"

      - name: Upload non-browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ozon-vm-path-audit
          path: ${{ runner.temp }}/vm-result.txt
          if-no-files-found: error
          retention-days: 7

  browser-path:
    name: Execute current browser path on Windows CFT 151
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact branch history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup exact Node
        uses: actions/setup-node@v4
        with:
          node-version: '24.12.0'

      - name: Reconstruct exact current candidate
        id: reconstruct
        continue-on-error: true
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $work = Join-Path $env:RUNNER_TEMP 'ozon-path-audit'
          $candidate = Join-Path $work 'candidate'
          $patch = Join-Path $work 'composer-wait.patch'
          if (Test-Path $work) { Remove-Item -Recurse -Force $work }
          New-Item -ItemType Directory -Force $candidate | Out-Null

          $zipHash = (Get-FileHash -Algorithm SHA256 $env:FROZEN_ZIP).Hash.ToLowerInvariant()
          if ($zipHash -ne $env:FROZEN_ZIP_SHA256) { throw "frozen zip sha mismatch: $zipHash" }
          $p0File = Join-Path $work '00.patch.part'
          $p1File = Join-Path $work '01.patch.part'
          cmd /c "git cat-file blob $env:PATCH_0_BLOB > `"$p0File`""
          if ($LASTEXITCODE -ne 0) { throw 'materialize patch part 0 failed' }
          cmd /c "git cat-file blob $env:PATCH_1_BLOB > `"$p1File`""
          if ($LASTEXITCODE -ne 0) { throw 'materialize patch part 1 failed' }
          $p0 = [System.IO.File]::ReadAllBytes($p0File)
          $p1 = [System.IO.File]::ReadAllBytes($p1File)
          $all = New-Object byte[] ($p0.Length + $p1.Length)
          [Array]::Copy($p0,0,$all,0,$p0.Length)
          [Array]::Copy($p1,0,$all,$p0.Length,$p1.Length)
          [System.IO.File]::WriteAllBytes($patch,$all)
          if ($all.Length -ne 13648) { throw "patch size mismatch: $($all.Length)" }
          $patchHash = (Get-FileHash -Algorithm SHA256 $patch).Hash.ToLowerInvariant()
          if ($patchHash -ne $env:PATCH_SHA256) { throw "patch sha mismatch: $patchHash" }
          Expand-Archive -LiteralPath $env:FROZEN_ZIP -DestinationPath $candidate -Force

          Push-Location $candidate
          git init -q
          git config core.autocrlf false
          git apply --check $patch
          git apply $patch
          $workerHash = (Get-FileHash -Algorithm SHA256 'service_worker.js').Hash.ToLowerInvariant()
          $contentHash = (Get-FileHash -Algorithm SHA256 'content_script.js').Hash.ToLowerInvariant()
          if ($workerHash -ne $env:FINAL_WORKER_SHA256) { throw "worker sha mismatch: $workerHash" }
          if ($contentHash -ne $env:FINAL_CONTENT_SHA256) { throw "content sha mismatch: $contentHash" }
          Pop-Location
          "CANDIDATE=$candidate" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host 'CURRENT_CANDIDATE_RECONSTRUCTION_PASS'

      - name: Install exact Puppeteer and CFT
        id: dependencies
        if: steps.reconstruct.outcome == 'success'
        continue-on-error: true
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $nodeproj = Join-Path $env:RUNNER_TEMP 'ozon-puppeteer-25.4.0'
          New-Item -ItemType Directory -Force $nodeproj | Out-Null
          Push-Location $nodeproj
          npm init -y | Out-Null
          $env:PUPPETEER_SKIP_DOWNLOAD='true'
          npm install --no-save --no-audit --no-fund puppeteer@25.4.0
          Pop-Location

          $zip = Join-Path $env:RUNNER_TEMP 'chrome-win64-151.0.7922.47.zip'
          $root = Join-Path $env:RUNNER_TEMP 'chrome-win64-151.0.7922.47'
          Invoke-WebRequest -Uri 'https://storage.googleapis.com/chrome-for-testing-public/151.0.7922.47/win64/chrome-win64.zip' -OutFile $zip
          Expand-Archive -LiteralPath $zip -DestinationPath $root -Force
          $chrome = Get-ChildItem -Path $root -Filter chrome.exe -File -Recurse | Select-Object -First 1
          if (-not $chrome) { throw 'CFT chrome.exe not found' }
          $version = (Get-Item $chrome.FullName).VersionInfo.ProductVersion
          if ($version -notlike '151.0.7922.47*') { throw "unexpected CFT version: $version" }
          "NODE_PATH=$nodeproj\node_modules" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "CFT_CHROME=$($chrome.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host "CFT_VERSION=$version"
          Write-Host 'CURRENT_BROWSER_DEPENDENCIES_READY'

      - name: Materialize current browser source for engineering audit
        id: materialize
        if: steps.reconstruct.outcome == 'success' && steps.dependencies.outcome == 'success'
        continue-on-error: true
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          git fetch --no-tags origin $env:READY_TEST_SOURCE_COMMIT
          $test = Join-Path $env:RUNNER_TEMP 'B10_B13_B15_BROWSER_CURRENT.mjs'
          cmd /c "git cat-file blob e3ea01ff6518626aa1fc9c96398b486cbb584b01 > `"$test`""
          if ($LASTEXITCODE -ne 0) { throw 'git cat-file browser blob failed' }
          node --check $test
          "BROWSER_TEST=$test" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          Write-Host 'CURRENT_BROWSER_SOURCE_SYNTAX_PASS'

      - name: Execute browser path
        id: execute
        if: steps.reconstruct.outcome == 'success' && steps.dependencies.outcome == 'success' && steps.materialize.outcome == 'success'
        continue-on-error: true
        shell: pwsh
        run: |
          $log = Join-Path $env:RUNNER_TEMP 'browser-execution.log'
          node $env:BROWSER_TEST $env:CANDIDATE $env:CFT_CHROME 2>&1 | Tee-Object -FilePath $log
          $rc = $LASTEXITCODE
          "BROWSER_EXIT_CODE=$rc" | Tee-Object -FilePath $log -Append
          if ($rc -ne 0) {
            'CURRENT_BROWSER_PATH_EXECUTE_FAIL' | Tee-Object -FilePath $log -Append
            exit $rc
          }
          'CURRENT_BROWSER_PATH_EXECUTE_PASS' | Tee-Object -FilePath $log -Append

      - name: Build browser evidence artifact
        if: always()
        shell: pwsh
        run: |
          $out = Join-Path $env:RUNNER_TEMP 'browser-result.txt'
          @(
            "trigger_sha=$env:GITHUB_SHA"
            "node=$(node --version 2>$null)"
            "reconstruct=${{ steps.reconstruct.outcome }}"
            "dependencies=${{ steps.dependencies.outcome }}"
            "materialize=${{ steps.materialize.outcome }}"
            "execute=${{ steps.execute.outcome }}"
            "frozen_zip_sha256=$env:FROZEN_ZIP_SHA256"
            "patch_sha256=$env:PATCH_SHA256"
            "final_worker_sha256=$env:FINAL_WORKER_SHA256"
            "final_content_sha256=$env:FINAL_CONTENT_SHA256"
            ''
          ) | Set-Content -LiteralPath $out -Encoding utf8NoBOM
          $log = Join-Path $env:RUNNER_TEMP 'browser-execution.log'
          if (Test-Path $log) { Get-Content -LiteralPath $log | Add-Content -LiteralPath $out -Encoding utf8 }
          Get-Content -LiteralPath $out

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ozon-browser-path-audit
          path: ${{ runner.temp }}/browser-result.txt
          if-no-files-found: error
          retention-days: 7

  publish-result:
    name: Publish one engineering preflight result
    if: always()
    needs: [vm-paths, browser-path]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch for evidence-only commit
        uses: actions/checkout@v4
        with:
          ref: dev/ozon-v0.1.19-manual-delivery-composer-wait-2026-08-18
          fetch-depth: 0

      - name: Download audit evidence
        uses: actions/download-artifact@v4
        with:
          pattern: ozon-*-path-audit
          path: ${{ runner.temp }}/audit-results

      - name: Publish exact engineering preflight result
        shell: bash
        run: |
          set -euo pipefail
          RESULT='tooling/llm-api-bridges/ozon-seller/validation/engineering-preflight/CURRENT_TEST_PATH_AUDIT_2026-08-19.md'
          mkdir -p "$(dirname "$RESULT")"
          VM=$(find "$RUNNER_TEMP/audit-results" -name vm-result.txt -print -quit || true)
          BR=$(find "$RUNNER_TEMP/audit-results" -name browser-result.txt -print -quit || true)
          {
            echo '# Ozon v0.1.19 — engineering pre-Codex current-path audit'
            echo
            echo "Trigger commit: \`$GITHUB_SHA\`"
            echo
            echo 'This is engineering preflight evidence only. It is not a Codex PASS and does not authorize packaging.'
            echo
            echo '## Non-browser current-hash path execution'
            echo '```text'
            if [ -n "$VM" ]; then cat "$VM"; else echo 'VM_RESULT_ARTIFACT_MISSING'; fi
            echo '```'
            echo
            echo '## Windows/CFT browser current-hash path execution'
            echo '```text'
            if [ -n "$BR" ]; then cat "$BR"; else echo 'BROWSER_RESULT_ARTIFACT_MISSING'; fi
            echo '```'
          } > "$RESULT"

          git config user.name 'github-actions[bot]'
          git config user.email '4189824+github-actions[bot]@users.noreply.github.com'
          git add "$RESULT"
          if git diff --cached --quiet; then
            echo 'engineering preflight result already identical'
            exit 0
          fi
          git commit -m 'test: record Ozon current path audit evidence [skip ci]'
          git push origin HEAD:dev/ozon-v0.1.19-manual-delivery-composer-wait-2026-08-18
